# URL de l'instance ntfy. https://ntfy.sh pour le service public,
# ou l'adresse de ton instance auto-hebergee (sans slash final).
NTFY_URL=https://ntfy.exemple.fr
# Jeton d'authentification, uniquement si l'instance en exige un
# (envoye en en-tete Authorization: Bearer). Laisser vide sur ntfy.sh public.
NTFY_TOKEN=tk_CHANGE-MOI
# Topic ntfy du coach — mets un suffixe aleatoire, un topic se devine !
NTFY_TOPIC_COACH=coach-velo-CHANGE-MOI-a1b2c3
# Jeton du chemin du calendrier .ics (openssl rand -hex 6)
ICS_TOKEN=a1b2c3d4e5f6
